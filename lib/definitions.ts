export type GeneralTagData = {
    id: string;
    name: string;
    color: string;
};

export type GeneralTagDataCreate = {
    name: string;
    color: string;
};

export type GeneralTagDataUpdate = {
    id: string;
    name: string;
    color: string;
};